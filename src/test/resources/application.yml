spring:
  profiles:
    include: common

  datasource:
    url: jdbc:h2:mem:untitled;mode=MYSQL;IFEXISTS=FALSE;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  h2:
    console:
      enabled: true

  jpa:
    hibernate:
      ddl-auto: create-drop
